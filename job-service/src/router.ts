import { Router } from "express";

const router = Router();

// router.use("/jobs/:jobId", 
// // Middleware for validating jobId is in a set of job IDs
// async (req, res, next) => {
//   // TODO: Queue job
//   // TODO: Respond with status
// })

export default router;
